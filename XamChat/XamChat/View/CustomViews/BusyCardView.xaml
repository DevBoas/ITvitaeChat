<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:d="http://xamarin.com/schemas/2014/forms/design"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       mc:Ignorable="d"
       BackgroundColor="Transparent"
       x:Class="ITvitaeChat2.View.NewFolder.BusyCardView">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="80*"/>
        </Grid.RowDefinitions>

        <!--Header-->
        <BoxView Grid.Row="0" CornerRadius="10,10,0,0" BackgroundColor="{StaticResource PrimaryColor}"/>
        <Label Grid.Row="0" Margin="16" Text="{Binding pLoadingMessageTitle, FallbackValue='No title found'}" TextColor="{StaticResource SecondaryTextColor}" FontAttributes="Bold" FontSize="Large"/>

        <!--Body-->
        <BoxView Grid.Row="1" CornerRadius="0,0,10,10" BackgroundColor="{StaticResource DisabledColor}"/>
        <Label Grid.Row="1" Margin="16" Text="{Binding pLoadingMessage, FallbackValue='No message found'}"/>
        <ActivityIndicator Grid.Row="1" Color="{StaticResource SecondaryDarkColor}" IsRunning="True" HorizontalOptions="Center" VerticalOptions="Center"/>
    </Grid>
</ContentView>